# المراجعة الأمنية — نظام الأندلس

> يوثّق هذا الملف المعالجات الأمنية، لمراجعة مسؤول التقنية.

## الثغرات التي أبلغ عنها مسؤول التقنية — وحالتها

### ✅ 1. كلمة المرور مدمجة في الكود (الثغرة الكبرى)
**قبل:** بيانات الدخول وكلمات المرور كانت في كود الواجهة (يراها أي أحد يفتح مصدر الصفحة).
**بعد:**
- كلمات المرور تُخزَّن في قاعدة البيانات **مُجزّأة بـ bcrypt** (12 جولة) — لا نص صريح إطلاقاً.
- شاشة الدخول تصادق عبر الخادم، لا بمقارنة محلية.
- عند الترحيل: كلمات المرور القديمة (نص صريح) تُجزّأ تلقائياً.

### ✅ 2. لا نظام قاعدة بيانات / لا CRUD حقيقي
**بعد:** قاعدة SQLite علائقية كاملة، كل العمليات عبر API مع استعلامات مُعامَلة.

## معالجات أمنية إضافية طُبّقت

| الإجراء | التفصيل |
|---------|---------|
| **المصادقة** | JWT موقّع، بمهلة انتهاء (12 ساعة افتراضياً) |
| **الأسرار** | كلها في `.env` خارج الكود؛ `JWT_SECRET` إلزامي في production (الخادم يرفض الإقلاع بدونه) |
| **SQL Injection** | استعلامات مُعامَلة (prepared statements) في كل مكان — لا دمج نصّي |
| **XSS/رؤوس** | `helmet` يضبط رؤوس HTTP الآمنة |
| **تخمين كلمات المرور** | `rate limiting`: 20 محاولة دخول كل 15 دقيقة |
| **كشف المعلومات** | رسالة دخول موحّدة (لا تكشف وجود الحساب)؛ معالج أخطاء لا يُظهر تفاصيل في production |
| **الصلاحيات** | كل مسار محمي بالدور المناسب على الخادم (لا يُتجاوز من المتصفح) |
| **إخفاء هوية المُقيّمين** | مفروض على الخادم — الموظف لا يستطيع رؤية من قيّمه ولو عدّل المتصفح |
| **حماية كلمة المرور في الاستجابات** | `password_hash` لا يُعاد أبداً في أي رد |
| **سجل تدقيق** | جدول `audit_log` يسجّل الدخول والعمليات الحسّاسة |
| **حجم الطلب** | محدود بـ 2MB لمنع الإغراق |

## ما يجب على مسؤول التقنية فعله قبل التشغيل

1. **توليد `JWT_SECRET` قوي:**
   ```bash
   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
   ```
   ووضعه في `.env`.

2. **تفعيل HTTPS** (Railway يوفّره تلقائياً على النطاقات الفرعية).

3. **بعد الترحيل:** مطالبة المستخدمين بتغيير كلمات المرور الافتراضية عند أول دخول.

4. **النسخ الاحتياطي الدوري** لملف قاعدة البيانات `andlus.db` (نسخ ملف واحد يكفي).

5. **مراجعة `CORS_ORIGIN`** في `.env` — تقييده لنطاق الواجهة فقط في production.

## توصيات مستقبلية (تحسينات، ليست ثغرات)
- إضافة "تغيير كلمة المرور" في واجهة كل مستخدم.
- سياسة قوة كلمة المرور (حدّ أدنى للطول والتعقيد).
- تدوير رمز JWT (refresh tokens) للجلسات الطويلة.
