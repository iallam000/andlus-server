# دليل تسليم منصّة الأندلس للتقييم 360° والتطوّر المهني

هذا الدليل موجّه لمسؤول التقنية لنشر المنصّة وتشغيلها. النظام جاهز للتشغيل، ويحتاج فقط إعدادات البيئة أدناه.

---

## 1. نظرة عامة على البنية

- **الخادم:** Node.js + Express + قاعدة بيانات SQLite (ملف واحد، لا تحتاج خادم قاعدة منفصلاً).
- **الواجهة:** مبنيّة مسبقاً في `public/app.bundle.js` (React مُجمّعة). تُخدَم كملف ثابت.
- **المصادقة:** JWT (رمز في الترويسة). كلمات المرور مشفّرة (bcrypt).
- **المنفذ الافتراضي:** 3000 (قابل للتغيير عبر `PORT`).

```
andlus-server/
├── app.js                    # نقطة الدخول
├── db/
│   ├── schema.sql            # مخطّط القاعدة (يُنشئ الجداول تلقائياً)
│   └── index.js              # تهيئة القاعدة والبذور
├── controllers/              # منطق الأعمال
├── routes/                   # مسارات API
├── config/
│   ├── auth.js               # التشفير والـ JWT
│   └── mailer.js             # إرسال البريد (لإعادة تعيين كلمة المرور)
├── middleware/auth.js        # حماية المسارات
├── public/                   # الواجهة المبنيّة + صفحة reset-password
└── scripts/
    ├── seed_admin.js         # إنشاء حساب مدير النظام
    └── build_ui.sh           # إعادة بناء الواجهة (عند التعديل)
```

---

## 2. خطوات النشر (أول مرة)

```bash
# 1) تثبيت الاعتماديات
npm install

# 2) ضبط متغيّرات البيئة (انظر القسم 3)
#    أنشئ ملف .env أو اضبطها في بيئة التشغيل

# 3) إنشاء حساب مدير النظام الأول
node scripts/seed_admin.js
#    ينشئ: admin / Admin@123  (يجب تغييرها فوراً بعد الدخول)

# 4) تشغيل الخادم
npm start
#    أو للإنتاج: pm2 start app.js --name andlus
```

القاعدة تُنشأ تلقائياً عند أول تشغيل (schema.sql). الجداول تشمل:
`users`, `evals`, `idps`, `impact`, `readings`, `locks`, `approvals`,
`account_requests` (طلبات فتح الحسابات), `password_resets` (إعادة تعيين كلمة المرور), `audit_log`.

---

## 3. متغيّرات البيئة

| المتغيّر | إلزامي | الوصف |
|---------|--------|-------|
| `JWT_SECRET` | **نعم** | مفتاح توقيع الرموز. **اجعله سرّياً وطويلاً وعشوائياً.** بدونه يُستخدم مفتاح تطوير غير آمن. |
| `PORT` | لا | منفذ الخادم (افتراضي 3000). |
| `DB_PATH` | لا | مسار ملف القاعدة (افتراضي داخل المشروع). ضعه في مسار دائم/محفوظ في الإنتاج. |
| `APP_BASE_URL` | موصى | العنوان العام للمنصّة (مثل `https://td.alandalus.edu.sa`). يُستخدم في رابط إعادة تعيين كلمة المرور بالبريد. |
| `SMTP_HOST` | للبريد | خادم البريد الصادر. |
| `SMTP_PORT` | للبريد | منفذ SMTP (587 عادةً، أو 465 لـ SSL). |
| `SMTP_USER` | للبريد | اسم مستخدم البريد. |
| `SMTP_PASS` | للبريد | كلمة مرور البريد (أو App Password). |
| `SMTP_FROM` | للبريد | عنوان المُرسِل الظاهر (مثل `no-reply@alandalus.edu.sa`). |

مثال ملف `.env`:
```
JWT_SECRET=ضع_هنا_سلسلة_عشوائية_طويلة_جداً
PORT=3000
DB_PATH=/var/data/andlus.db
APP_BASE_URL=https://td.alandalus.edu.sa
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=no-reply@alandalus.edu.sa
SMTP_PASS=••••••••
SMTP_FROM=no-reply@alandalus.edu.sa
```

---

## 4. الربط ببريد الشركة (مهمّ — لميزة «نسيت كلمة السر»)

### كيف تعمل الميزة
1. اسم المستخدم في المنصّة **هو بريد الموظف الفعلي في الشركة**.
2. عند الضغط «نسيت كلمة السر؟» وإدخال البريد، يولّد الخادم رمزاً مؤقّتاً (صالح **ساعة واحدة**) ويرسل رابطاً على ذلك البريد.
3. الموظف يفتح الرابط (`/reset-password?token=...`)، يُدخل كلمة مرور جديدة، فتُحدَّث.

### ما الذي يجب أن يراعيه مسؤول التقنية
**نعم، هناك أمور تحتاج ضبطاً ليعمل الإرسال فعلياً:**

1. **تثبيت مكتبة الإرسال:**
   ```bash
   npm install nodemailer
   ```
   (المنصّة تعمل بدونها، لكن الرابط سيُطبع في سجلّ الخادم بدل إرساله — مفيد للاختبار فقط.)

2. **ضبط متغيّرات SMTP الخمسة** (القسم 3) ببيانات بريد الشركة. الخيارات الشائعة:
   - **Microsoft 365 / Exchange:** `smtp.office365.com` منفذ 587. قد يتطلّب إنشاء **App Password** أو حساب خدمة مخصّص، والسماح بـ SMTP AUTH للحساب (قد يكون معطّلاً افتراضياً في المؤسسة).
   - **Google Workspace:** `smtp.gmail.com` منفذ 587 + App Password (مع تفعيل التحقّق بخطوتين).
   - **خادم بريد داخلي:** استخدم بياناته.

3. **حساب مُرسِل مخصّص:** يُفضّل إنشاء صندوق مثل `no-reply@alandalus.edu.sa` بدل حساب شخصي، وضبطه في `SMTP_USER`/`SMTP_FROM`.

4. **ضبط `APP_BASE_URL`** على العنوان العام الصحيح، وإلا سيحوي الرابط عنوان الخادم الداخلي ولن يفتح من خارج الشبكة.

5. **قابلية الوصول للرابط:** الرابط يشير لصفحة `/reset-password` على نفس المنصّة، فتأكّد أن المنصّة متاحة على الإنترنت (أو شبكة الموظفين) عبر `APP_BASE_URL`.

6. **بريد الموظفين مضبوط بدقّة:** بما أن اسم المستخدم = البريد، تأكّد عند إنشاء الحسابات أن البريد صحيح ومفعّل ويستقبل رسائل خارجية.

7. **مجلد الوارد/المزعج:** جرّب أن رسالة إعادة التعيين لا تقع في «الرسائل غير المرغوبة». قد يلزم إضافة سجلّ SPF/DKIM لنطاق المُرسِل.

### ملاحظة أمنية (مصمّمة عمداً)
عند طلب إعادة التعيين، يردّ الخادم **بنفس الرسالة** سواء كان البريد مسجّلاً أم لا («إن كان الحساب موجوداً فسيصلك رابط») — هذا **متعمّد** لمنع كشف الحسابات الموجودة. لا تغيّره.

### اختبار سريع للبريد
بعد الضبط، جرّب «نسيت كلمة السر» بحساب حقيقي. إن لم يصل البريد، راجع سجلّ الخادم: إن ظهر الرابط مطبوعاً فالإرسال غير مضبوط (SMTP)، وإن ظهر خطأ SMTP فالمشكلة في بيانات الاتصال/الصلاحيات.

---

## 5. تغيير كلمة المرور (للمستخدم المسجَّل)
منفصلة عن «نسيت كلمة السر». من لوحته، يُدخل الموظف: كلمة المرور الحالية + الجديدة + تأكيدها. تعمل عبر `/api/auth/change-password` وتتحقّق من الحالية بشكل مشفّر. **لا تحتاج بريداً.**

---

## 6. طلبات فتح الحسابات
مدير الفرع/الإدارة يطلب فتح حساب لموظف في نطاقه (بكلمة مرور مبدئية بسيطة يحدّدها)، ومدير النظام يعتمد الطلب فيُنشأ الحساب. يغيّر الموظف كلمته لاحقاً من لوحته. لا إعداد إضافيّاً مطلوباً.

---

## 7. إعادة بناء الواجهة (عند تعديلها مستقبلاً)
الواجهة المصدرية ملفّ واحد (`AndlusIDP360.jsx`). لإعادة بنائها بعد أي تعديل:
```bash
bash scripts/build_ui.sh /path/to/AndlusIDP360.jsx
```
ينتج `public/app.bundle.js` المحدّث.

---

## 8. النسخ الاحتياطي
القاعدة كلها في ملف واحد (`DB_PATH`). انسخه دورياً (مثلاً cron يومي). لا حاجة لأدوات نسخ معقّدة.

---

## نقاط تحقّق قبل الإطلاق
- [ ] `JWT_SECRET` عشوائي وسرّي.
- [ ] `DB_PATH` في مسار دائم (لا يُمسح مع إعادة النشر).
- [ ] `APP_BASE_URL` صحيح ومتاح.
- [ ] `nodemailer` مثبّت + SMTP مضبوط ومُختبَر.
- [ ] تغيير كلمة مرور `admin` الافتراضية.
- [ ] نسخ احتياطي دوري للقاعدة.
- [ ] بريد الموظفين صحيح عند إنشاء الحسابات.
