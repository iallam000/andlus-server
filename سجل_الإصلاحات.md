# سجلّ إصلاحات ملاحظات المراجعة الجزئية

هذه الإصلاحات على النسخة التي أرسلتموها. الأهمّ منها **إصلاح قاعدة البيانات** الذي يحلّ تلاشي الحفظ.

---

## 🔴 الإصلاح الأهمّ — تلاشي حفظ التقييمات (الجذر الأكبر)

**السبب:** جدول `eval_scores` في قاعدة البيانات كان **ينقصه عمود `round`** (رقم جولة التقييم). دالة الترحيل `rebuildEvalScores` في `db/index.js` كانت تعيد بناء الجدول **بدون هذا العمود**، رغم وجوده في `schema.sql`. فعند كل حفظ تقييم، ينفّذ الخادم `INSERT ... round ...` الذي **يفشل** لأن العمود غير موجود في القاعدة الفعلية → الحفظ لا يكتمل → البيانات تتلاشى عند الخروج.

**لماذا كان سجلّ الخادم نظيفاً؟** لأن الخطأ يقع داخل معاملة (transaction) قد تبتلعه، والأهمّ أن القراءة تعمل (فتبدو البيانات موجودة في الجلسة ثم تختفي).

**الإصلاح:**
- `db/index.js`: `rebuildEvalScores` الآن يشمل عمود `round` (مع ترحيل القيم القديمة كجولة 1).
- أُضيف ترحيل صريح يضيف عمود `round` لأي قاعدة ينقصها، وكذلك عمودَي `certificate` (للخطط) و`init_password` (لطلبات الحسابات).
- `public/api.js`: دالة `saveEval` المباشرة تُرسل الآن `round` بشكل صحيح (كان يُرسَل خطأً باسم `witnesses`).

### ⚠️ خطوة مطلوبة عند النشر
عند تشغيل الخادم بالكود الجديد، ستُطبَّق الترحيلات تلقائياً وتُضيف الأعمدة الناقصة. **لا حاجة لحذف القاعدة**، لكن إن أردتم بداية نظيفة يمكن إعادة تهيئتها. تأكّدوا أن الخادم شُغّل مرة واحدة بعد التحديث ليطبّق الترحيل قبل الاستخدام.

---

## 🟠 "التقييم مغلق رغم فتحه"

**السبب:** لوحة الموظف كانت تقرأ `isOpen` من جذر كائن نوافذ التقييم، بينما الحالة مخزّنة **لكل فرع** في `branches[فرع_الموظف].isOpen`. فكانت القيمة `undefined` دائماً → يظهر "مغلق".

**الإصلاح:** `AndlusIDP360.jsx` — لوحة الموظف تستخرج الآن نافذة فرعها: `w.branches[user.branch]`.

---

## 🟡 تحصينات إضافية
- `ResultsReadingSection` (في البطاقة): حُصّنت ضد `currentUser` الناقص لتفادي انهيار البطاقة عند فتحها من مستوى أعلى.

---

## ملاحظات وُجدت سليمة في الكود المرسَل (لا تحتاج إصلاحاً برمجياً)

بعد الفحص الدقيق، هذه كانت **سليمة** في النسخة المرسَلة، وأعراضها السابقة كانت من الجذر الأكبر (فشل الحفظ) أو تشغيلية:

- **حفظ الخطط والشهادات الاحترافية:** مربوط بالخادم بشكل صحيح (`saveIdpsDiff`, `profCerts`). كان يتأثّر بفشل الحفظ العام؛ يعمل بعد إصلاح قاعدة البيانات.
- **طلبات فتح الحساب (تبقى معلّقة):** منطق الاعتماد يُعيد قراءة الحالة من الخادم بشكل صحيح، والخادم يحدّث الحالة في معاملة واحدة. يعمل صحيحاً.
- **قائمة المراحل في نموذج الطلب:** قائمة منسدلة من `STAGES` (سليمة).
- **الفروع التابعة في نموذج الطلب:** النموذج يعرض قائمة منسدلة إن كان لمدير الفرع أكثر من فرع. **هذا يعتمد على البيانات:** يجب أن يُسنِد مدير النظام **عدّة فروع** (`branches`) للمدير عند إنشاء حسابه (عبر أداة الاختيار المتعدّد في شاشة المستخدمين). إن أُسند فرع واحد فقط، يظهر فرع واحد — وهذا سلوك صحيح.

## البريد (نسيت كلمة السر)
تشغيلي — يحتاج `npm install nodemailer` + ضبط متغيّرات SMTP و`APP_BASE_URL` (راجع دليل التسليم). بدونها يُطبع رابط إعادة التعيين في سجلّ الخادم.

---

## الملفات المعدّلة
- `db/index.js` — ترحيل عمود round وأعمدة أخرى.
- `db/schema.sql` — عمود round + certificate.
- `public/api.js` — إرسال round في saveEval.
- `controllers/evalsController.js` — قبول كل الأطراف + الجولتين (كان سليماً في الأغلب).
- `controllers/idpsController.js` — حفظ الشهادة.
- `AndlusIDP360.jsx` + `public/app.bundle.js` — إصلاح نافذة التقييم + تحصين البطاقة.
